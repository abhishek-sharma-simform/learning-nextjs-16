@AGENTS.md

# Project

Next.js 16 (App Router) learning project using React 19, TypeScript, and Tailwind CSS 4.

- Dev server: `npm run dev`
- Lint: `npm run lint`
- Routes live under `app/` (e.g. `about`, `blog`, `products`, `docs`, `(auth)`).
